# Flutter/Dart/Pub related
**/doc/api/                  # Exclude generated API documentation
**/ios/Flutter/.last_build_id # Exclude Flutter's build ID
.dart_tool/                  # Exclude Flutter-specific tool configurations
.flutter-plugins-dependencies # Exclude plugin dependencies
.pub-cache/                  # Exclude Flutter package cache
.pub/                        # Exclude Flutter pub packages
/build/                      # Exclude build outputs
/coverage/                   # Exclude code coverage reports

# Flutter/Android specific
/android/app/debug/          # Exclude Android debug build artifacts
/android/app/profile/        # Exclude Android profile build artifacts
/android/app/release/        # Exclude Android release build artifacts

# Symbolication & Obfuscation related
app.*.symbols                # Exclude crash symbol files
app.*.map.json               # Exclude obfuscation map files

# IDE files (JetBrains, VS Code, etc.)
*.iml                        # IntelliJ IDEA project files
*.ipr
*.iws
.idea/                       # Exclude all IntelliJ IDEA files
.vscode/                     # Uncomment to ignore VS Code settings (if not needed)

# macOS-specific files
.DS_Store                    # Exclude macOS Finder metadata

# Miscellaneous files
*.class                      # Java class files
*.log                        # Log files (can contain sensitive info)
*.pyc                        # Python bytecode files (if used)
*.swp                        # Vim swap files
.atom/                       # Atom configuration folder
.svn/                        # Exclude SVN metadata
.swiftpm/                    # Exclude Swift package manager files
migrate_working_dir/         # Exclude working directory for migrations

# Build-related directories
.build/                      # Exclude general build directories
.buildlog/                   # Exclude build logs
.history                     # Exclude history files for version control

# Personal or machine-specific files
local.properties             # Exclude local Android properties (can contain sensitive info)
*.keystore                   # Exclude keystore files (do NOT push keystore to GitHub)
*.jks                        # Exclude Java Keystore files

# Linux/Windows/Windows-specific files
Thumbs.db                    # Windows thumbnail cache files
desktop.ini                  # Windows-specific metadata files
*.bak                        # Backup files (sometimes generated by editors)
